export type Infos = {
    date: Date
    ratio: number
    download: number
    upload: number
}

export type Config = {
    address: string
    login_path: string
    user: string
    password: string
    result_file: string
    cookie_key: string
}